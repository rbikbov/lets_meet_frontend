// https://cli.vuejs.org/guide/deployment.html#github-pages
// const REPO = 'base_app_frontend';

// module.exports = {
export default {
  // publicPath: import.meta.env.PROD ? `/${REPO}/` : '/',
  // publicPath: process.env.NODE_ENV === 'production' ? `/${REPO}/` : '/',
};
